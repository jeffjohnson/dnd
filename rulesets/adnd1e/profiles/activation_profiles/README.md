# Activation Profiles

Activation profiles describe which optional publications, variants, and replacements are active at query time. They do not define the `ruleset_id` or alter canonical graph assertions.
